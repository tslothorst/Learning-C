#include <stdio.h>
#include <string.h>

struct item{
    char* itemName;
    int quantity;
    float price;
    float amount;
};

void readItem(struct item* obj);
void printItem(struct item* obj);

int main() {

    return 0;
}
